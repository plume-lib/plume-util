version=2.0.0-SNAPSHOT

org.gradle.jvmargs=-Xmx6g
org.gradle.caching=true

# The configuration cache cannot be enabled yet:  the org.checkerframework Gradle plugin stores a
# Project reference in a JavaCompile task action, which the configuration cache cannot serialize.
# To see the problem, run:  ./gradlew build --configuration-cache
# org.gradle.configuration-cache=true

# https://vanniktech.github.io/gradle-maven-publish-plugin/central/
mavenCentralPublishing=true
signAllPublications=true
